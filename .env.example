# Copy to .env and fill in. The app runs in LOCAL-ONLY mode if these are absent:
# capture and offline persistence work, but nothing syncs to the backend.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# --- Deferred (AI layer, not used in the foundation slice) ---
# Cost guardrail for the future Claude layer. Kept here so config exists before the
# backend functions are built. Enforced server-side, not in the client.
# CLAUDE_MONTHLY_SPEND_CAP_USD=50
